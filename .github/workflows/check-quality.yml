name: Check Quality Code

on:
  push:
    branches:
      - 'master'

jobs:
  build:

    runs-on: amazoncorretto
    permissions:
      contents: read
      packages: write

    steps:
    - name: Run test
      run: mvn test 
